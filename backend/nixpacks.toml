[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "source $HOME/.local/bin/env && uv sync --no-dev"
]

[start]
cmd = "~/.local/bin/uv run uvicorn main:app --host 0.0.0.0 --port $PORT"